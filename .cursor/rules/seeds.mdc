---
description: DB seeds — N/A in portal; use migrations repo
globs: src/seeds/**
alwaysApply: false
---

# Seeds (N/A — portal)

Repo này **không** chứa DB seed scripts. Data seeding qua `nalee-sports-migrations/src/seeds/`.

Portal chỉ đọc/ghi qua GraphQL backend — không seed trực tiếp MongoDB.

Nếu thêm `src/seeds/` trong tương lai: idempotent upsert, respect dry-run, rate-limit API calls.
